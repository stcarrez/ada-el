with "util";

project El is
   Version := "1.1.0";

   for Library_Name use "adael";
   for Library_Dir use "/usr/lib";
   for Library_Kind use "static";

   for Source_Dirs use ("/usr/share/ada/adainclude/el");
   for Library_ALI_Dir use "/usr/share/ada/adainclude/el";
   for Externally_Built use "true";
end El;
